[pytest]
pythonpath = main .
testpaths = other/tests
markers =
    integration: requires a real PostgreSQL database or multiple runtimes
    e2e: exercises the complete Gateway/runtime/device protocol
    deployment: validates migration, restart, readiness, and rollout behaviour
