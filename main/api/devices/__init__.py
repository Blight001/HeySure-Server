"""Device-related helpers: bindings, live state, MCP permissions, presence.

Import from the sub-modules directly:
    from api.devices.presence import online_tool_defs
    from api.devices.bindings import get_binding, set_binding
    from api.devices.live import emit_agent_list_for_user
    from api.devices.mcp_permissions import get_scope, set_scope
"""
